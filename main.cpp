#include <iostream>
#include "mathtools.hpp"    // paste in the declarations so main knows these exist

int main() {
    std::cout << "add(3, 4) = " << add(3, 4) << "\n";
    std::cout << "square(5) = " << square(5) << "\n";
    return 0;
}